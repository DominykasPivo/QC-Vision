/**
 * Camera component constants
 * Centralized configuration for camera controls and settings
 */

export const ZOOM_CONFIG = {
  MIN: 1,
  MAX: 3,
  STEP: 0.1,
  DEFAULT: 1,
};

export const CAPTURE_CONFIG = {
  DEFAULT_QUALITY: 0.92,
  DEFAULT_FORMAT: "image/jpeg",
};
